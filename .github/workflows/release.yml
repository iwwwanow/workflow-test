name: Release
on:
  push:
    branches:
      - master
      - beta

permissions:
  contents: read # for checkout

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      contents: write # to be able to publish a GitHub release
      issues: write # to be able to comment on released issues
      pull-requests: write # to be able to comment on released pull requests
      id-token: write # to enable use of OIDC for trusted publishing and npm provenance
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*"
      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npm install --no-save semantic-release @semantic-release/commit-analyzer @semantic-release/release-notes-generator @semantic-release/github @semantic-release/git
          npx semantic-release
  # docker:
  #   name: Build and Push Docker Image
  #   needs: release  # Критически важно: ждем создания тега
  #   runs-on: ubuntu-latest
  #   # Разрешения для GHCR
  #   permissions:
  #     contents: read
  #     packages: write
  #  
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0  # Нужно для получения тегов
  #    
  #     # Шаг 1: Получаем тег (созданный semantic-release)
  #     - name: Get release tag
  #       id: tag
  #       run: echo "TAG=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
  #    
  #     # Шаг 2: Настраиваем Docker Buildx
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
  #    
  #     # Шаг 3: Логин в GHCR
  #     - name: Login to GHCR
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}
  #    
  #     # Шаг 4: Сборка и пуш образа
  #     - name: Build and push
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         push: true
  #         tags: |
  #           ghcr.io/${{ github.repository }}:${{ steps.tag.outputs.TAG }}
  #           ghcr.io/${{ github.repository }}:latest
